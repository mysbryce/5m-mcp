#!/usr/bin/env node
"use strict";var import_node_fs=require("node:fs"),import_node_path=require("node:path");function report(payload){process.stdout.write(JSON.stringify(payload)+`
`)}function isLikelyNui(url){let u=url.toLowerCase();return!(u.startsWith("devtools://")||u.startsWith("chrome://")||u.startsWith("chrome-untrusted://")||u==="about:blank")}async function listTargets(cdpUrl){let r=await fetch(`${cdpUrl.replace(/\/$/,"")}/json`);if(!r.ok)throw new Error(`CDP /json returned ${r.status}`);return(await r.json()).filter(p=>p.type==="page"&&!!p.webSocketDebuggerUrl)}var CdpClient=class{ws;nextId=1;pending=new Map;constructor(wsUrl){let WS=globalThis.WebSocket;if(!WS)throw new Error("WebSocket not available (need Node 22+).");this.ws=new WS(wsUrl),this.ws.addEventListener("message",ev=>{try{let msg=JSON.parse(typeof ev.data=="string"?ev.data:"");if(typeof msg.id!="number")return;let cb=this.pending.get(msg.id);cb&&(this.pending.delete(msg.id),cb(msg))}catch{}})}ready(timeoutMs){return new Promise((resolve,reject)=>{if(this.ws.readyState===1){resolve();return}let t=setTimeout(()=>reject(new Error("ws open timeout")),timeoutMs);this.ws.addEventListener("open",()=>{clearTimeout(t),resolve()}),this.ws.addEventListener("error",e=>{clearTimeout(t);let msg=e.message??"unknown";reject(new Error(`ws error: ${msg}`))})})}send(method,params={},timeoutMs=1e4){let id=this.nextId++;return new Promise((resolve,reject)=>{let t=setTimeout(()=>{this.pending.delete(id),reject(new Error(`CDP ${method} timed out after ${timeoutMs}ms`))},timeoutMs);this.pending.set(id,msg=>{clearTimeout(t),msg.error?reject(new Error(`${method}: ${msg.error.message}`)):resolve(msg.result)}),this.ws.send(JSON.stringify({id,method,params}))})}close(){try{this.ws.close()}catch{}}};async function findIframeRect(client,name){var _a;let expr=`(() => {
    const sel = ${JSON.stringify(`iframe[name="${name.replaceAll('"','\\"')}"]`)};
    const el = document.querySelector(sel);
    if (!el) return null;
    const r = el.getBoundingClientRect();
    return { x: r.x, y: r.y, width: r.width, height: r.height };
  })()`;return((_a=(await client.send("Runtime.evaluate",{expression:expr,returnByValue:!0})).result)==null?void 0:_a.value)??null}async function hideSiblingIframes(client,keepName){var _a;let expr=`(() => {
    const keep = ${JSON.stringify(keepName)};
    const hidden = [];
    for (const f of document.querySelectorAll('iframe')) {
      if (f.getAttribute('name') === keep) continue;
      f.dataset.agentApiPrevDisplay = f.style.display || '';
      f.style.display = 'none';
      hidden.push(f.getAttribute('name') || '');
    }
    return hidden;
  })()`;return((_a=(await client.send("Runtime.evaluate",{expression:expr,returnByValue:!0})).result)==null?void 0:_a.value)??[]}async function restoreSiblingIframes(client){await client.send("Runtime.evaluate",{expression:`(() => {
    for (const f of document.querySelectorAll('iframe')) {
      if (f.dataset.agentApiPrevDisplay !== undefined) {
        f.style.display = f.dataset.agentApiPrevDisplay;
        delete f.dataset.agentApiPrevDisplay;
      }
    }
    return true;
  })()`,returnByValue:!0}).catch(()=>{})}async function main(){let outputPath=process.argv[2],cdpUrl=process.argv[3]??"http://localhost:13172",timeoutMs=Number(process.argv[4]??"15000"),targetFilter=(process.argv[5]??"").toLowerCase(),iframeName=process.argv[6]??"",mode=(process.argv[7]??"isolate").toLowerCase();outputPath||(report({ok:!1,error:"usage: screenshot-nui <outputPath> [cdpUrl] [timeoutMs] [targetFilter] [iframeName] [mode=isolate|clip|full]"}),process.exit(2));try{(0,import_node_fs.mkdirSync)((0,import_node_path.dirname)(outputPath),{recursive:!0})}catch(e){report({ok:!1,error:`mkdir failed: ${e.message}`}),process.exit(2)}let targets;try{targets=await listTargets(cdpUrl)}catch(e){report({ok:!1,error:`cannot reach CDP at ${cdpUrl}: ${e.message}`}),process.exit(3)}targets.length===0&&(report({ok:!1,error:`no page targets exposed by ${cdpUrl}. Ensure the FiveM client is running.`}),process.exit(4));let chosen;targetFilter&&(chosen=targets.find(p=>p.url.toLowerCase().includes(targetFilter))),chosen||(chosen=targets.find(p=>isLikelyNui(p.url))),chosen||(chosen=targets[0]);let client=new CdpClient(chosen.webSocketDebuggerUrl),restoredHidden=[];try{await client.ready(timeoutMs);let clip=null,iframeReport;if(iframeName){let rect=await findIframeRect(client,iframeName);iframeReport={name:iframeName,found:!!rect,rect},rect||(report({ok:!1,error:`iframe[name="${iframeName}"] not found in ${chosen.url}. Is the resource's UI open?`,iframe:iframeReport}),client.close(),process.exit(7)),(rect.width<1||rect.height<1)&&(report({ok:!1,error:`iframe[name="${iframeName}"] has zero size (${rect.width}x${rect.height}) \u2014 UI is likely hidden.`,iframe:iframeReport}),client.close(),process.exit(8)),mode==="clip"&&(clip=rect),mode==="isolate"&&(restoredHidden=await hideSiblingIframes(client,iframeName))}let screenshotParams={format:"png",fromSurface:!0};clip&&(screenshotParams.clip={...clip,scale:1});let res=await client.send("Page.captureScreenshot",screenshotParams,timeoutMs);if(!res.data)throw new Error("CDP response missing result.data");let buffer=Buffer.from(res.data,"base64");(0,import_node_fs.writeFileSync)(outputPath,buffer);let bytes=(0,import_node_fs.statSync)(outputPath).size??buffer.length;report({ok:!0,path:outputPath,bytes,mode,target:{url:chosen.url,title:chosen.title,id:chosen.id},candidates:targets.map(p=>({url:p.url,title:p.title})),iframe:iframeReport,hiddenSiblings:restoredHidden})}catch(e){report({ok:!1,error:e instanceof Error?e.message:String(e)}),process.exit(5)}finally{restoredHidden.length>0&&await restoreSiblingIframes(client),client.close()}}main();
